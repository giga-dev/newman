apiVersion: v1
kind: Service
metadata:
  name: newman-server
  namespace: newman
  labels:
    app: newman-server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    app: newman-server
